# GitHub Workflows (Archived)

These workflows were previously considered for use for CI and GitHub Pages deployment.

The project is now deployed on **Azure App Service**, which provides a full Node.js
runtime for Next.js (SSR, API routes, middleware, server actions, etc.).

These workflow files are kept for reference only and are marked as **disabled**.
They no longer run automatically.